#pragma once
#include <string>

using namespace std;

class parser {
public:
	parser();								//constructs a parser object
	int commandType(string command);		//takes a token from inputReader and outputs the 
private:

};